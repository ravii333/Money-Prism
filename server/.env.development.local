# MongoDB connection string (local or cloud)
MONGODB_URI=mongodb://localhost:27017/moneyprism-dev

# App environment
NODE_ENV=development

# Frontend (React) base URL
CLIENT_URL=http://localhost:3000

# Port to run the server
PORT=5000
