import React from 'react';

const Footer = () => (
  <footer className="bg-indigo-800 text-white text-center py-4">
    © {new Date().getFullYear()} PartWise — Save Smarter, Shop Better
  </footer>
);

export default Footer;
